# RunPod research log — 2026-09-08

Timezone: Asia/Seoul (KST, UTC+09:00)

## 23:05 — Pod and GPU status check

- SSH connection succeeded.
- Pod GPU: NVIDIA A40, 46,068 MiB total VRAM.
- At the time of the check the GPU was idle: 0 MiB used, 0% utilization.
- The Pod itself was running, so RunPod billing could still accrue while GPU
  compute was idle.
- DAVIS was present at `/workspace/CMMT/data/DAVIS`.
- Remote CMMT checkout was at commit `4c2793d`.

## 23:06 — Logging policy

- User requested KST timestamps in intermediate progress updates.
- Record meaningful commands, results, failures, fixes, and artifact locations
  in this log.
- Keep SSH keys, checkpoints, DAVIS data, raw state dumps, and bulk masks out of
  Git. Commit only reproducible code, configuration, documentation, metrics,
  and representative small previews.

## 23:07 — DAVIS sequence validation

- Selected `blackswan` as the first single-object DAVIS validation sequence.
- DAVIS validator confirmed 50 RGB frames and the frame-0 annotation.
- Fixed switch frame 10, leaving 39 future frames for continuation comparison.

## 23:08–23:12 — DAVIS same-checkpoint round-trip

- Ran Tiny export→inject→Tiny and Large export→inject→Large on the A40.
- Tiny: mean binary IoU 0.9998837611, mean logit MSE 0.0113338014,
  wall time 26.69 s, peak CUDA memory 755,715,584 B.
- Large: mean binary IoU 0.9999313743, mean logit MSE 0.0010071985,
  wall time 45.48 s, peak CUDA memory 1,655,799,296 B.
- Both runs made zero prefix-backbone calls before/during injection and exactly
  39 future-backbone calls.
- Gate result: behavioral round-trip passed for this one-sequence, one-object
  DAVIS case. The logits were not bit-identical, so exact state equivalence is
  not claimed.
- Raw JSON summaries were copied to
  `reports/experiments/2026-09-08_davis_roundtrip_a40/`.

## 23:14 — Git preparation

- Created commit `da35015` for the final CUDA build-isolation fix and the rule
  that excludes `.runpod_ssh/` from Git.
- The earlier venv bootstrap fix is commit `47e1602`.
- User explicitly authorized pushing the prepared project commits and selected
  small experiment artifacts to the current feature branch.

## 23:25–23:31 — Selective baseline implementation and remote verification

- Added repeatable `--translator` selection to `cmmt-paired-experiment`, so the
  first real-data run can execute Direct and Ridge without training Linear/MLP.
- Added serializable Ridge parameters and explicit DAVIS `--split train|val|none`
  validation.
- Local and RunPod test suites both passed: `17 passed`.
- Committed and pushed code as `e0f9466`; fast-forwarded `/workspace/CMMT` to the
  same commit.
- At the post-test status check the A40 was idle: 0 MiB used, 0% utilization.

## 23:31–23:34 — Second paired checkpoint state

- Validated DAVIS train sequence `bmx-bumps`: 90 frames, frame-0 annotation
  present.
- Ran official-checkpoint Tiny and Large through frames 0–6 with object id 1 and
  seed 7.
- Saved canonical states to RunPod
  `/workspace/CMMT/outputs/paired_states/bmx-bumps_switch6/`; each `.pt` is about
  11 MB and remains outside Git.
- Compatibility reports found 47 matched manifest rows and equal boundary shapes;
  semantic equivalence is not inferred from shape equality.

## 23:34–23:35 — First real DAVIS Direct/Ridge offline comparison

- Fit Ridge on the `bear` Tiny/Large state pair and evaluated on held-out
  `bmx-bumps`; Direct Copy was evaluated on the same held-out pair.
- Spatial memory MSE: Direct 3.0086, Ridge 0.8187; cosine: 0.0365 vs 0.8093.
- Object pointer MSE: Direct 0.6973, Ridge 0.4891; cosine: -0.0215 vs 0.4662.
- Presence-logit MSE worsened from 1.1686 to 4.0098, making the unweighted
  component-average MSE 9.1% worse for Ridge. This aggregate is not a downstream
  model-selection metric.
- Direct/Ridge median CPU translation latency was 0.100/1.109 ms; Ridge had
  69,954 parameters.
- Actual target injection and future-mask J&F were not run by this offline command.
- Small reports were copied to
  `reports/experiments/2026-09-08_davis_paired_state_pilot/`.
- After completion the A40 again reported 0 MiB used and 0% utilization; the Pod
  was still running and potentially billable.

## 23:40–23:45 — Ridge-hybrid injection implementation

- Added Ridge payload reload and the hybrid policy: Ridge spatial memory,
  Ridge object pointer, Direct presence logit.
- Generalized the checkpoint-backed cross-model handoff runner and added
  `sam2-ridge-handoff-smoke`.
- Local and RunPod verification passed (`18 passed`); code commits were
  `9375fbd` and later evaluation commits `df7ecf8`, `18a1ecf`.

## 23:45–23:52 — Actual `bmx-bumps` Direct and Ridge-hybrid handoffs

- Both runs used Tiny prefix frames 0–6 and Large continuation frames 7–89.
- Both made zero target-backbone calls before/during injection and 83 calls only
  for future frames.
- Large-native agreement: Direct mean binary IoU 0.5616458 and mean logit MSE
  121,852.47; hybrid IoU 0.9493896 and MSE 1.8787.
- First future frame IoU improved from 0.2460 to 0.8582.
- Direct/hybrid full experiment wall time was 90.89/91.91 s and both peaked at
  1,660,043,264 CUDA bytes.
- Full artifacts are 55/54 MB on RunPod. Only JSON, Markdown and four selected
  comparison images per method were copied to Git-friendly local reports.

## 23:54 — Visual artifact verification

- Opened Direct and hybrid comparisons for frames 7, 44, 50 and 78.
- Visual overlays matched the reported behavior: hybrid was much closer to
  Large-native at the switch and reappearance frames; frame 50 also showed why
  native agreement must not be confused with ground truth.

## 23:56–00:15 — Official DAVIS metric-function evaluation

- Cloned official `davisvideochallenge/davis2017-evaluation` at commit
  `ac7c43fca936f9722837b7fbd337d284ba37004b` (114 MB) under `.external`.
- Installed only the needed metric dependencies in the project venv:
  `opencv-python-headless`, `scikit-image` and their required packages.
- Added reproducible `cmmt-davis-future-eval`; local/RunPod tests passed
  (`19 passed`).
- An initial 7–89 partial evaluation was corrected because official
  semi-supervised protocol excludes the video last frame. Final reported scope
  is frames 7–88 (82 frames).
- One attempted shell loop failed because PowerShell expanded remote shell
  variables before SSH. Its outputs were rejected; three explicit commands were
  rerun successfully.

## 2026-09-09 00:38–00:46 — Frame 50 GT 위치 및 시각화 개선

- 원본 DAVIS annotation에서 object 1의 27픽셀을 다시 확인했다. 원본 854×480
  좌표 범위는 `x=204–221, y=404–408`이다.
- 기존 4-panel은 Input / Large-native / candidate / agreement였으므로 GT가
  직접 표시되지 않았음을 확인했다.
- 이후 생성되는 비교 PNG는 첫 패널에 평가 대상 object의 GT를 빨강으로 overlay
  하도록 변경했다. 관련 artifact test는 로컬에서 `1 passed`였다.
- 현재 Git-friendly pilot 요약에는 4개 대표 프레임만 있고, 전체 83-frame
  artifacts(Direct 55 MB, hybrid 54 MB)는 RunPod `/workspace/CMMT/outputs/`에 있다.
- 기존 inference 결과만 읽는 CPU 후처리 스크립트 `scripts/build_handoff_review.py`로
  GT / Large-native / Direct / Ridge-hybrid 4-panel 83장, 슬라이더 HTML, MP4,
  frame별 J&F 그래프를 생성했다. GPU 사용량은 0%/0 MiB였고 모델 추론 결과는
  변경하지 않았다.
- RunPod 결과는 `/workspace/CMMT/outputs/handoffs/review_bundle`, 로컬 결과는
  `outputs/review_bundle`이다. 로컬 최종 크기는 23,074,027 bytes이며 `outputs/`는
  Git ignore 대상이다. manifest와 83개 이미지 존재를 확인했고 전체 테스트는
  `19 passed`였다.
- HTML은 `frames.json`을 fetch하지 않고 frame 목록을 자체 포함하도록 갱신해,
  로컬 파일 직접 열기에서도 별도 web server가 필요 없게 했다. SSH 파일 복사와
  같은 elevated 명령에 이어 실행한 첫 pytest는 Windows 기본 temp 권한 때문에
  setup 오류가 났으나, 프로젝트 내부 `outputs/pytest-temp`를 지정한 재실행은
  `19 passed`였다. 이는 코드/실험 결과 오류가 아니다.
- 2026-09-09 01:41 KST에 GitHub Pages source를
  `kim/exp-sam2-state-translator:/docs`로 활성화했다. 첫 deployment run
  `34252571719`는 33초에 성공했고 공개 gallery에서 slider를 frame 7→50으로
  변경했을 때 이미지와 J&F가 함께 갱신되는 것을 확인했다.
- 공개 URL: `https://memorybridge-team.github.io/vos-memory-translator/`
- Final partial-sequence J&F: Large-native 0.862989, Direct 0.638527, hybrid
  0.860456. Hybrid gained 0.221929 over Direct and was 0.002533 below
  Large-native.
- Official metric code emitted its known empty-union divide warning before
  replacing both-empty J with 1; evaluation completed successfully.
- At the last status check the A40 was idle (0 MiB, 0%); the Pod remained running
  and potentially billable.

## 00:16 — Final synchronization and resource status

- Pushed and synchronized result commit `3fa6c5c` on branch
  `kim/exp-sam2-state-translator` to both GitHub and `/workspace/CMMT`.
- RunPod project storage usage by major directory: DAVIS data 1.1 GB,
  checkpoints 1,008 MB, `.external` 423 MB, outputs 166 MB (about 2.7 GB total).
- A40 status was 0 MiB used and 0% utilization. No further GPU computation was
  queued; the running Pod could still accrue charges until stopped in RunPod.

## 2026-09-09 01:47–02:00 KST — 저장소 정리 검토와 안내 문서 개편

- 현재 정식 계획, 재현성, GitHub Pages 공개 결과, 제3자 자료의 중복 여부를
  기준으로 작업 branch의 tracked files를 전수 검토했다.
- 제3자 논문 PDF 3개(약 23.6MB)와 페이지별 추출 전문은 Git 추적에서 제외하고,
  `references/README.md`의 공식 출처 링크로 대체하기로 했다. 로컬 연구용 원문은
  지우지 않고 ignored cache로 남긴다.
- 이전 smoke/pilot 보고서는 실행 이력과 실패 조건을 검증하는 근거이므로
  유지했다. 83-frame Pages gallery 역시 다른 컴퓨터에서 결과를 확인하기 위한
  필수 산출물로 유지했다.
- 루트 README를 한국어로 다시 작성해 연구 목적, 현재 검증 범위, 공개 결과,
  다음 baseline/Translator 실험, 성공 기준과 실행 방법을 한 화면에서 찾게 했다.
- 과거 Translator 계획은 보관본으로 표시하고, 현재 계획은
  `docs/experimental_plan.md`를 기준으로 삼았다. RunPod 문서의 과거 예산 cap은
  제거하고 연구 완전성 우선 정책과 GPU/CPU 역할 분리를 반영했다.
- Direct/Ridge 하위 보고서가 Git에 없는 전체 frame 이미지를 링크하던 기존 문제를
  발견해 실제 보존된 대표 4프레임만 표시하고 83-frame Pages gallery로 연결했다.
- 로컬 Markdown 37개의 내부 링크를 검사했고 모두 유효했다. 프로젝트 가상환경의
  전체 test는 `19 passed in 2.72s`였다.

## 2026-09-09 02:10–02:16 KST — 새 RunPod endpoint 및 volume 자산 점검

- 이전 SSH endpoint는 timeout이었고 사용자가 제공한 새 endpoint로 재접속했다.
  새 host key는 Git에서 제외되는 프로젝트 전용 `.runpod_ssh/known_hosts`에만
  저장했다.
- A40은 49,140MiB 중 0MiB, utilization 0%였다. `/workspace`는 공유 filesystem
  전체 수치를 표시하므로 project별 `du`를 별도로 확인했다.
- `/workspace/CMMT`는 약 3.8GB다. 주요 항목은 DAVIS 1.1GB, checkpoint
  1,008MB, `.external` 423MB, outputs 214MB, `.venv` 940MB, `.git` 83MB다.
- DAVIS train 60개·val 30개 split, Tiny 149MB·Large 857MB checkpoint,
  pinned SAM 2 `2b90b9f5...`, DAVIS evaluator `ac7c43fc...`, paired state 4개와
  Ridge artifact가 모두 있어 다음 실험에 필요한 persistent 자산 누락은 없다.
- RunPod checkout에 남은 untracked gallery script는 최신 Git 버전보다 DAVIS
  attribution 한 줄만 빠진 구본임을 hash/diff로 확인한 뒤 제거했다. 데이터,
  checkpoint와 실험 결과는 삭제하지 않았다.
- RunPod branch를 `45c11a8`로 fast-forward했고 project directory에서 전체 test
  `19 passed in 5.33s`를 확인했다.
- 다음 작업은 deterministic DAVIS event manifest 생성기와 동일 manifest를 쓰는
  Reset/Last-Mask/Replay-k/Full-Replay baseline runner 구현이다. 시작 직전 Codex
  사용량이 5시간 89%, 주간 14%로 확인되어 합의한 80% 보호선에 따라 새 장기
  작업을 시작하지 않고 이 재개 지점을 저장했다.

## 2026-09-09 14:24–14:42 KST — 병목 방지용 manifest 및 공통 case cache

- 사용량 reset 이후 시작 상태는 5시간 3%, 주간 15%였다. 작업 후 확인값은
  5시간 26%, 주간 19%로, 합의한 80% 보호선 아래다. Reset credit은 사용하지 않았다.
- DAVIS case를 결정적으로 선택하는 manifest 생성기, policy config와 checksum
  검증을 구현해 commit `97a7e39`로 push했다. 로컬 test는 당시 `22 passed`였다.
- 데이터 I/O는 hot/warm/cold로 분리했다. 활성 DAVIS frame·checkpoint·state는
  RunPod persistent volume에 유지해 GPU critical path에서 원격 작은 파일 I/O를
  제거하고, Git에는 코드·config·작은 manifest/metric만 둔다. 완료된 대형 파일의
  object-storage 업로드는 GPU 작업과 분리한다.
- 동일 case를 baseline마다 처음부터 재실행하던 compute 병목을 제거하기 위해
  source Tiny prefix와 target Large-native oracle을 한 번만 준비하는 checksummed
  case cache를 구현했다. Direct/Ridge 평가는 이후 Large continuation만 실행하며
  공통 준비 비용과 candidate-only 비용을 분리 기록한다.
- Cache는 CPU tensor와 path-independent metadata만 저장하고 atomic rename과
  SHA-256 sidecar로 불완전 파일을 거부한다. 전체 로컬 test는 `26 passed in 2.64s`,
  관련 module `py_compile`과 `git diff --check`도 통과했다.
- `69.30.85.97:22054` SSH endpoint는 두 차례 timeout이 발생했다. 따라서 actual
  DAVIS val manifest 생성과 real-checkpoint cache smoke는 아직 실행하지 않았으며,
  Pod가 다시 연결되면 수행할 다음 검증 단계로 남겼다.
