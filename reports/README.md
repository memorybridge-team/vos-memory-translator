# Experiment reports

Small reproducibility reports and selected visual previews may be committed here
so that collaborators can inspect results directly in GitHub or VS Code.

Keep checkpoints, datasets, raw state tensors, and bulk predictions in ignored
storage. A standard run directory contains `report.json`, `report.md`, binary
mask previews, and a small number of comparison PNGs.
